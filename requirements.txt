# ═══════════════════════════════════════════════════════════════════════════════
#  ShadeMusicBot — requirements.txt
#  Verified against PyPI on 2026-08-05. All versions confirmed to exist.
# ═══════════════════════════════════════════════════════════════════════════════

# ─── Telegram MTProto ──────────────────────────────────────────────────────────
# pyrofork: actively maintained pyrogram fork. Installs as "pyrogram" namespace.
# TgCrypto-pyrofork: matching crypto extension (installs as "TgCrypto" namespace).
pyrofork>=2.3.0
TgCrypto-pyrofork>=1.2.5

# ─── Voice Chat Streaming ─────────────────────────────────────────────────────
# Package name on PyPI : py-tgcalls   (NOT "pytgcalls" — that is a different,
#                                       older, unmaintained package)
# Import name in code  : pytgcalls    (from pytgcalls import PyTgCalls)
# Latest stable        : 2.3.3        (released Jun 16, 2026)
# Python >=3.10 required; Python 3.12 supported (verified on PyPI classifiers).
#
# <3.0.0.dev0 excludes pre-release 3.0.0.dev* builds which use a completely
# different API (currently in development as of Aug 2026).
py-tgcalls==2.3.3

# ntgcalls: C++ WebRTC backend for py-tgcalls.
# Latest stable : 2.2.5 (released Jun 22, 2026)
# Pre-built wheels: manylinux_2_28_x86_64 (Linux x86_64, matches Render).
# Pinned explicitly so pip always resolves the correct binary wheel.
ntgcalls==2.2.5

# ─── YouTube / Audio extraction ────────────────────────────────────────────────
# Never pin yt-dlp — YouTube changes its API regularly; stale yt-dlp = broken.
# Floor ensures modern extractor_args / player_client support.
yt-dlp>=2025.1.0

# ─── Database ──────────────────────────────────────────────────────────────────
motor==3.4.0
pymongo==4.7.2

# ─── Web / Health ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ─── Configuration ─────────────────────────────────────────────────────────────
pydantic-settings==2.3.1
pydantic==2.7.4
python-dotenv==1.0.1

# ─── Logging ───────────────────────────────────────────────────────────────────
loguru==0.7.2

# ─── Utilities ─────────────────────────────────────────────────────────────────
python-dateutil==2.9.0
